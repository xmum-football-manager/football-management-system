# Documentation Contradiction Review

> Auto-generated by recursive review of all docs. Review and resolve before full Phase 1 build.

---

## CRITICAL BLOCKING ISSUES

These need resolution before implementing the affected features:

1. **Format lock timing** — FR-28 says lock at `first_match_scheduled_at`; UX dashboard (§6.1, §10.1.5) proposes lock at `status = active` (first match goes Live). Which is authoritative?
2. **Organizer creation authority** — PRD only grants Admins the ability to create tournaments; UX dashboard (§10.1.4) proposes giving Organizers this ability. Is this Phase 1 or Phase 2?
3. **Hybrid format availability** — PRD is silent on `round_robin_knockout`. UX dashboard and tournament-forms.md include it. Is it Phase 1 or Phase 2?
4. **Points system form** — `tournament-forms.md` says two presets only; UX dashboard (§5.4) proposes a "Custom..." third option. What's Phase 1 scope?
5. **Dashboard routing** — `pages.md` shows separate `/edit` page with old tab structure; UX dashboard (§4.3) removes `/edit` and puts edits on Overview tab. Which model is correct?
6. **Polling interval** — PRD NFR-03 says 5 seconds; `realtime.md` and `ux-spec.md` say 30-second fallback. Are these the same thing (Realtime primary + 30s fallback), or is 5s polling still required?
7. **Hybrid tournament columns** — UX dashboard (§5.2, §6.2) refers to `number_of_groups`, `teams_per_group`, `teams_advancing_per_group` — are these columns in the schema or computed?
8. **Admin audit log schema** — `schema.md` references `admin_audit_log` but never defines its columns, types, or example entries.
9. **Match notes persistence** — `ux-spec.md` (Fixtures tab) mentions a "Match notes" free-text field but no `notes` column appears in `schema.md`. Is this stored in the DB?
10. **Scorekeeper RLS `scores only`** — `rls.md` says scorekeepers can UPDATE `matches` "scores only when live." Is this a DB-level column restriction, or just enforced in the UI?

---

## 1. FORMAT AND TOURNAMENT STRUCTURE

### 1.1 Format options — conflicting definitions
**Files:** `prd.md`, `ux-dashboard.md` (§5.2), `tournament-schema-and-rules.md`, `tournament-forms.md`

- **PRD (FR-17):** Only two modes — round-robin ON = league, OFF = knockout.
- **UX Dashboard + Schema + Forms:** Three values: `round_robin`, `knockout`, `round_robin_knockout`.
- **Gap:** PRD doesn't mention the hybrid format at all.

### 1.2 Format lock timing — version conflict
**Files:** `prd.md` (FR-28), `ux-dashboard.md` (§6.1, §10.1.5), `tournament-schema-and-rules.md`, `tournament-edit-page.md`

- **PRD FR-28:** Lock when `first_match_scheduled_at` is set.
- **UX Dashboard:** Lock when `status = active` (first match goes Live).
- **Tournament Edit Page:** Lock when `setup` AND `first_match_scheduled_at IS NULL`.
- **Three different triggers across three docs.**

### 1.3 Organizer tournament creation
**Files:** `prd.md` (FR-07), `ux-dashboard.md` (§5.2, §10.1.4), `pages.md`

- **PRD:** Only Admins assign Organizers to tournaments; creation authority unclear.
- **UX Dashboard:** Proposes Organizers can create their own tournaments (auto-assigned).
- **Pages.md:** "+ New Tournament" is "(admin only, or organizer if given create access)".

---

## 2. TOURNAMENT STATUS LIFECYCLE

### 2.1 Setup → Active transition
**Files:** `prd.md`, `ux-dashboard.md` (§7.1), `ux-spec.md`

- **UX Dashboard (§7.1):** Proposes a confirmation modal before the first match goes Live — not in the original PRD.
- **Question:** Is this modal a Phase 1 requirement?

### 2.2 Active → Finished transition
**Files:** `prd.md`, `ux-dashboard.md` (§6.7, §7.1), `ux-spec.md`

- **PRD:** No "Mark tournament as finished" FR listed.
- **UX Dashboard + UX Spec:** Include a "Mark as finished" button when 100% matches are `finished`.
- **Question:** Is this in Phase 1 scope?

---

## 3. SCOREKEEPER ROLE

### 3.1 Scorekeeper scope in `user_roles`
**Files:** `prd.md` (FR-15, FR-26), `user-role-assignment.md`, `schema.md`

- **Schema:** Scorekeeper has either `tournament_id` set (tournament-wide) OR `match_id` set (match-specific) — not both.
- **PRD FR-26:** Organizer can assign scorekeeper to "a specific match or the entire tournament."
- **Question:** For match-specific scorekeeper, is `tournament_id` null? If so, how does RLS know which tournament they belong to for permission checks?

### 3.2 Scorekeeper access during halftime
**Files:** `scorekeeper-halftime-fix.md`, `ux-spec.md`

- **Halftime fix doc:** Match appears in scorekeeper list with `halftime` status; `canScore('halftime')` returns false.
- **UX Spec:** Goal buttons hidden during `halftime`, showing "Half Time — waiting for 2nd half."
- **Question:** Is the match visible but non-editable (preferred UX per halftime-fix doc), or should it disappear from the list entirely?

---

## 4. ORGANIZER ASSIGNMENT

### 4.1 Organizer with no tournament assigned
**Files:** `user-role-assignment.md`, `organizer-tournament-assignment.md`, `ux-dashboard.md`

- Organizer is created with `tournament_id = null`.
- **Gap:** No doc describes the empty-state UX — what does the Organizer see before being assigned to any tournament? Options: empty message, request-assignment link, create tournament form, error state?

---

## 5. POINTS SYSTEM

### 5.1 Custom points — scope conflict
**Files:** `prd.md` (FR-12), `tournament-forms.md`, `ux-dashboard.md` (§5.4)

- **Tournament Forms:** Two radio presets only — `3/2/1` or `1/0.5/0`.
- **UX Dashboard:** Recommends adding "Custom..." as a third option.
- **PRD FR-12:** "Organizer can change the values per tournament" (implies custom input).
- **Question:** Are arbitrary decimal values supported in Phase 1, or only the two presets?

---

## 6. ADMIN PERMISSIONS

### 6.1 Admin delete tournament at any status
**Files:** `prd.md` (FR-23), `ux-dashboard.md` (§6.7), `rls.md`

- **PRD FR-23:** Danger zone delete available to "Admin, Organizer."
- **UX Dashboard (§6.7):** Available to Admin at any status; Organizer only during `setup`.
- **Question:** Can Admins delete a `finished` or `active` tournament?

---

## 7. MATCH LIFECYCLE

### 7.1 Halftime → Live transition safeguard
**Files:** `schema.md`, `ux-spec.md`

- No `current_half` column on matches; UI must infer "2nd half now" from transition sequence.
- **Gap:** What happens if Organizer presses "Start 2nd half" twice? Is the match still in `halftime`? No safeguard documented.

### 7.2 `match_time` display on public view
**Files:** `schema.md`, `organizer-score-input.md`, `pages.md`

- `match_time` = display-only scheduled estimate; `match_started_at` = authoritative actual start.
- **Gap:** On `/t/[id]`, does the public see `match_time`, `match_started_at`, or both? If the match starts 15 minutes late, what is displayed?

---

## 8. FRONTEND ROUTING

### 8.1 Tournament detail navigation model
**Files:** `pages.md`, `ux-dashboard.md` (§4.2, §4.3)

- **Pages.md:** Separate `/admin/tournaments/[id]/edit` page; tab structure includes stat cards.
- **UX Dashboard:** Removes `/edit`; edits happen inline on Overview tab; default tab at `setup` is Overview.
- **These are fundamentally different routing models.** Which one is being built?

### 8.2 Default tab on tournament open
**Files:** `ux-dashboard.md` (§4.2), `ux-spec.md`, `pages.md`

- **UX Dashboard:** Default tab = Overview when `status = setup`; Fixtures when `active`.
- **Pages.md:** Implies Fixtures is always the landing (stat cards + match list at root route).

---

## 9. LOCK RULES

### 9.1 Name edit — 14-day rule intent
**Files:** `tournament-schema-and-rules.md`, `ux-dashboard.md` (§6.1)

- Both docs say name is editable if `start_date − today > 14d`.
- **Unusual:** An active tournament's name can still change if the start date was far ahead. Is this intentional, or should `active` always lock the name regardless of date?

### 9.2 Format/points lock trigger (again)
**Files:** `tournament-schema-and-rules.md`, `tournament-edit-page.md`, `ux-dashboard.md` (§6.1)

- Three different lock triggers across three docs (see §1.2 above). Summarized here for completeness.

---

## 10. REAL-TIME AND POLLING

### 10.1 Polling interval
**Files:** `prd.md` (NFR-03), `ux-spec.md`, `realtime.md`

- **PRD:** 5-second polling.
- **Realtime + UX Spec:** Supabase Realtime primary; 30-second fallback.
- **Question:** Is 5-second polling mentioned in the PRD a legacy requirement superseded by Realtime, or is 5s still required somewhere?

### 10.2 Admin dashboard polling vs. Realtime
**Files:** `ux-spec.md` (§8.4), `realtime.md`

- **Realtime doc:** Describes Realtime subscription for `/t/[id]` (participant view).
- **UX Spec:** Suggests admin Fixtures/Standings/Bracket tabs use polling with "Updated Xs ago" indicator, not Realtime.
- **Question:** Is the admin dashboard intentionally polling (not Realtime), or should it also subscribe?

---

## 11. SCORE INPUT

### 11.1 Simultaneous organizer + scorekeeper edits
**Files:** `organizer-score-input.md`, `ux-spec.md` (§/score)

- Organizer can edit scores from Fixtures tab (localStorage cache).
- Scorekeeper has dedicated `/score` page.
- **Gap:** No conflict resolution documented. If both edit simultaneously, which write wins? Does the scorekeeper see the organizer's cached (not-yet-saved) scores?

### 11.2 Scorekeeper vs. organizer localStorage cache
**Files:** `organizer-score-input.md`, `ux-spec.md`

- Organizer score input uses localStorage for optimistic cache.
- Scorekeeper `/score` page doesn't mention localStorage.
- **Question:** Are these intentionally different implementations?

---

## 12. HYBRID FORMAT STRUCTURE

### 12.1 Missing schema columns for hybrid groups
**Files:** `ux-dashboard.md` (§5.2, §6.2), `schema.md`

- UX Dashboard refers to `number_of_groups`, `teams_per_group`, `teams_advancing_per_group`.
- These columns do not appear in `schema.md`.
- **Question:** Are they missing from the schema doc, or not yet designed?

### 12.2 Group constraint enforcement
**Files:** `ux-dashboard.md` (§6.2)

- Rule: Can't start tournament until every group has ≥ 2 teams.
- **Gap:** Where is this enforced — UI only, API layer, or DB constraint? What's the exact error message?

### 12.3 Group stage vs. knockout match scope
**Files:** `ux-dashboard.md` (§6.3)

- During hybrid format, fixture creation dropdowns scope to teams in the same group (group stage) or all teams (knockout).
- **Gap:** How does the system know a match is "group stage" vs. "knockout"? Is there a `phase` or `group_id` column on `matches`? Not in `schema.md`.

---

## 13. ADMIN AUDIT LOG

### 13.1 Log schema not defined
**Files:** `schema.md`, `ux-spec.md` (Flow C)

- `admin_audit_log` is referenced but never defined.
- **Missing:** Exact columns, types, what actions are logged beyond "Revert to Live," example rows.

---

## 14. PUBLIC PARTICIPANT VIEW

### 14.1 Multiple simultaneous live matches
**Files:** `pages.md`, `screens.md`, `ux-spec.md`

- Hero shows "most recently started live match."
- **Gap:** If multiple matches are simultaneously live, only one is shown in the hero. The others appear in Fixtures. This behavior is implied but not explicitly stated anywhere.

### 14.2 Bracket tab for hybrid format
**Files:** `pages.md`, `ux-spec.md`, `screens.md`

- **Pages.md:** Bracket tab visible for knockout format only.
- **Screens.md:** Bracket tab hidden for round-robin.
- **Gap:** For hybrid format, when is the Bracket tab shown — always, only during knockout phase, or never during group stage? Not stated.

---

## 15. FORM VALIDATION

### 15.1 "Same time" definition for team scheduling
**Files:** `pages.md`, `ux-spec.md`

- Rule: "Cannot schedule a team already playing at the same time."
- **Ambiguity:** Does "same time" mean (A) exact same `match_time` timestamp, (B) overlapping time windows, or (C) same time slot? Match durations aren't tracked in the schema.

### 15.2 Duplicate fixture rules in round-robin
**Files:** `ux-spec.md`

- Rule: "No duplicate fixture in round-robin if already played."
- **Question:** Does this block creation when a prior match with the same two teams is `scheduled` (not yet played), or only when the prior match is `finished`?

---

## 16. INFRASTRUCTURE

### 16.1 Keep-alive cron syntax
**Files:** `prd.md`, `github-actions-keepalive.md`, `plan-8-week.md`

- All three say "every 5 days."
- **Cron in doc:** `0 0 */5 * *` = "midnight on days 5, 10, 15, 20, 25, 30" (not exactly every 5 days from any given point).
- **Question:** Is the cron expression intentional, or should it be `0 0 * * */5` (every 5th day-of-week) or a different expression?

---

## 17. AUTHENTICATION

### 17.1 `must_change_password` flag persistence
**Files:** `auth.md`, `ux-spec.md` (Change Password), `pages.md`

- On login with `must_change_password = true`, redirect to `/change-password`.
- **Gap:** If the user closes the browser before completing the change, are they forced to change again on next login? The flag's lifecycle (when it's cleared) is not documented.

---

## 18. STANDING TIEBREAKERS

### 18.1 Goal difference tiebreaker implementation
**Files:** `prd.md` (FR-18), `ux-dashboard.md` (§5.2)

- Requirement: goal difference as tiebreaker.
- **Gap:** Is this handled in raw SQL (view/materialized view) or in the application layer? Not specified.

---

## 19. RLS POLICIES

### 19.1 Public SELECT on tournaments
**Files:** `rls.md`

- Statement: "`tournaments`: Public (anon + auth)."
- **Ambiguity:** Does anon (unauthenticated) user get `SELECT *` access, or only specific columns? The participant view `/t/[id]` needs this, but exact policy condition not shown.

---

## 20. MISSING DOCUMENTATION

### 20.1 Organizer empty-state UX
No doc describes what an Organizer sees on first login with no tournaments assigned.

### 20.2 Match notes field storage
`ux-spec.md` mentions "Match notes (free text, optional)" on the Fixtures tab. No `notes` column appears in `schema.md`. Where is this stored?

### 20.3 PWA install prompt ownership
`ux-dashboard.md` (§11) delegates PWA install prompt to the participant view spec — which is not in this doc set. Who implements it?

---

## 21. EDGE CASES

### 21.1 Scorekeeper assigned after match goes live
- Can a scorekeeper be assigned to a match that is already `live`? RLS allows organizer assignment, but is there a UI or API restriction?

### 21.2 Deleting a team with scheduled matches
- `ux-spec.md` (Teams tab): Teams locked at `active` status.
- **Gap:** What about `scheduled` matches in `setup` status — if a team is removed, does the DB cascade-delete their matches or reject the deletion?

### 21.3 Web Share API fallback
- `pages.md` / `ux-spec.md`: WhatsApp share uses `navigator.share`.
- **Gap:** No fallback behavior specified for browsers that don't support Web Share API.

---

## 22. DEFERRED / PHASE 2 ITEMS (explicitly open)

From `ux-dashboard.md` (§12):

- **Zero-scorekeeper offline scenario:** Mitigation acknowledged (organizer can input scores directly from Fixtures) but exact error/fallback UX not documented.
- **Knockout bracket seeding:** Auto-seed by group-stage finish position vs. overall points. Default proposal exists but needs confirmation at Phase 2 build.
- **Two-organizer simultaneous edit conflict:** Deferred until observed in production.
